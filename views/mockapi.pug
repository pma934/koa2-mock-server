extends layout

block content
  - var mockapi= location + '/mockapi'
  -
    var getMap = [
      {
        url:'/star?min={min}&max={max}',
        explain:'随机生成min~max颗星星',
        example:'/star?min=5&max=10'
      },
      {
        url:'/regexp?regexp={encodeURIComponent(regexp)}',
        explain:'随机生成与正则表达式匹配的字符串',
        example:'/regexp?regexp='+encodeURIComponent(/^\w+([-+.]\w+)*@\w+([-.]\w+)*\.\w+([-.]\w+)*$/)  
      },
      {
        url:'/people',
        explain:'5000条人物数据',
        example:'/people'
      },
      {
        url:'/image',
        explain:'5000张多尺寸图片URL',
        example:'/image'
      },
      {
        url:'/basicData',
        explain:'随机生成500条各种基础类型数据',
        example:'/basicData'
      },
      {
        url:'/complexData',
        explain:'随机生成的复杂数据',
        example:'/complexData'
      },
    ]
  h1= location
  p Welcome to #{title}
  ul
    each val in getMap
      li.example
        p.explain= val.explain
        p.url
          b GET&nbsp;&nbsp;
          =val.url
        a(href='/mockapi'+val.example)= mockapi+val.example

  p.explain 根据传入的参数生成一张图片
  p.url
    b POST&nbsp;&nbsp;
    | /customImage

  p(
    class="codepen" 
    data-height="510" 
    data-theme-id="0" 
    data-default-tab="js,result" 
    data-user="pma934" 
    data-slug-hash="NVYjVW" 
    style=`
      height: 265px; 
      box-sizing: border-box; 
      display: flex; 
      align-items: center; 
      justify-content: center; 
      border: 2px solid; 
      margin: 1em 0; 
      padding: 1em;`
    data-pen-title="主页面"
    ) codepen加载失败 